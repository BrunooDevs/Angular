import { ClientesssComponent } from './clientesss.component';

describe('ClientesssComponent', () => {
  it('should create an instance', () => {
    expect(new ClientesssComponent()).toBeTruthy();
  });
});
