import { CrearcuentaComponent } from './crearcuenta.component';

describe('CrearcuentaComponent', () => {
  it('should create an instance', () => {
    expect(new CrearcuentaComponent()).toBeTruthy();
  });
});
